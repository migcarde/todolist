import 'package:data/operations/item/item_remote_data_source.dart';
import 'package:data/operations/item/item_service.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mockito/annotations.dart';
import 'package:mockito/mockito.dart';

import 'item_remote_data_source_test.mocks.dart';

@GenerateMocks([ItemService])
void main() {
  /* var remoteDataSource;
  var mockService = MockItemService();

  setUp(() {
    remoteDataSource = ItemRemoteDataSource(itemService: mockService);
  });

  final jsonResponse = 

  test('Get items - Success', () async {
    // Given
    when(mockService.getItems()).thenAnswer((realInvocation) => null)
  }); */
}
